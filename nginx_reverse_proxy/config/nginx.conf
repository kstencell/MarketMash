# server {

#     error_log  /var/log/nginx/error.log;
#     access_log /var/log/nginx/access.log;

#     listen 80;
#     listen [::]:80;

#     root /app/build;

#     location / {
#         index /index.html;
#     }
# }

server {
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    server_name _;

    listen 80 default_server;
    listen [::]:80 default_server;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    # use the certificates
    ssl_certificate     /etc/letsencrypt/live/marketmash.ca/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/marketmash.ca/privkey.pem;
    server_name marketmash.ca;

    root /app/build;

    location / {
        index /index.html;
    }

    location ~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

# server {
#     listen 443 default_server ssl http2;
#     listen [::]:443 ssl http2;

#     server_name marketmash.ca;

#     ssl_certificate /etc/nginx/ssl/live/marketmash.ca/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/marketmash.ca/privkey.pem;
    
#     location / {
#         index /index.html;
#     }
# }